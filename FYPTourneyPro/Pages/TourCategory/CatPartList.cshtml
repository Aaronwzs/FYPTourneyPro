@page "{categoryId:guid}"
@model FYPTourneyPro.Pages.TourCategory.CatPartList
@{
    ViewData["Title"] = "Category Participants List";
}
@section scripts {
    <abp-script src="/Pages/TourCategory/CatPartList.js" />
}
<div class="container">
    <abp-card>
        <abp-card-header>
            <abp-card-title>
                Participants in @Model.Category.Name
            </abp-card-title>
        </abp-card-header>
        <abp-card-body>


            <h3>Registered Users</h3>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Category</th>
                            <th>Player 1</th>
                            <th>Player 2</th>
                            <th>Registration Date</th>
                            <th>Total Amount</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var registration in Model.Registrations)
                        {
                            <tr>
                                <td>@registration.CategoryName</td>
                                <td>@registration.userName1</td>
                                <td>@registration.userName2 ?? "N/A"</td>
                                <td>@registration.RegDate.ToString("yyyy-MM-dd")</td>
                                <td>@registration.TotalAmount</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </abp-card-body>
    </abp-card>
</div>